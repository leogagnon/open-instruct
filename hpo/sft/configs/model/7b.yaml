# @package _global_
model_name_or_path: allenai/OLMo-2-1124-7B
tokenizer_name: allenai/OLMo-2-0425-1B  # same tokenizer as 1B

# 4 GPUs × 1 × 32 = 128 effective batch size
per_device_train_batch_size: 1
gradient_accumulation_steps: 32
learning_rate: 1e-5
