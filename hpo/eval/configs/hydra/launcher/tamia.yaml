defaults:
  - submitit_slurm

mem_gb: 300
gpus_per_node: 4
array_parallelism: 32
nodes: 1
tasks_per_node: 1
cpus_per_task: 12
timeout_min: 179
account: aip-glaj
additional_parameters:
  no-requeue: true

setup:
  - "source ~/links/scratch/open-instruct/olmes/.venv/bin/activate"
  - "export SLURM_CPU_BIND=none"
